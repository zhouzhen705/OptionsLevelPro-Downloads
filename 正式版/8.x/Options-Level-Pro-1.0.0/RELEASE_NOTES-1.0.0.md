<!-- zh -->
# Options Level Pro 1.0.0 版本说明

目标版本：ATAS 8.x

Options Level Pro 1.0.0 是面向 ATAS 8.x 用户的正式版本。

## 本次更新

- 修复图表偶发显示 `0.00` 水平线的问题，只绘制有效的期权关键位。
- 将默认数据连接切换到 Trading Hub 正式 HTTPS 服务。
- 改进历史缓存读取方式，使用完整历史数据文件。
- 为 ATAS 8.x 提供独立 DLL 下载文件。

## 使用建议

请下载与你的 ATAS 版本匹配的 DLL。ATAS 7.x、ATAS 8.x 和 ATAS X 文件不能混用。

<!-- en -->
# Options Level Pro 1.0.0 Release Notes

Target version: ATAS 8.x

Options Level Pro 1.0.0 is the stable release for ATAS 8.x users.

## What's New

- Fixed the occasional `0.00` horizontal line so only valid options key levels are drawn.
- Switched the default data connection to the official Trading Hub HTTPS service.
- Improved history-cache loading with the full history data file.
- Provides a dedicated ATAS 8.x DLL download.

## Recommended Use

Download the DLL that matches your ATAS version. ATAS 7.x, ATAS 8.x, and ATAS X files are not interchangeable.
